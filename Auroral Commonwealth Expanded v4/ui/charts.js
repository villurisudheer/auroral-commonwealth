const palettes={gdp:'#72d7ff',inflation:'#ffcf67',debt:'#ad7cff',unemployment:'#ff8a98',population:'#4fe0a8',industry:'#80bfff'};
export function drawChart(canvas,history,key,label){if(!canvas)return;const dpr=devicePixelRatio||1;const w=canvas.clientWidth||600,h=canvas.clientHeight||220;canvas.width=w*dpr;canvas.height=h*dpr;const c=canvas.getContext('2d');c.scale(dpr,dpr);c.clearRect(0,0,w,h);c.strokeStyle='#1f3b55';c.lineWidth=1;for(let i=1;i<5;i++){c.beginPath();c.moveTo(36,(h-28)*i/5);c.lineTo(w-12,(h-28)*i/5);c.stroke()}const vals=history.slice(-60).map(x=>x[key]).filter(Number.isFinite);if(vals.length<2){c.fillStyle='#8ea8bf';c.fillText('Not enough history yet',20,30);return}let min=Math.min(...vals),max=Math.max(...vals);if(min===max){min*=.95;max*=1.05}const pad=(max-min)*.1;min-=pad;max+=pad;c.strokeStyle=palettes[key]||'#72d7ff';c.lineWidth=2.2;c.beginPath();vals.forEach((v,i)=>{const x=36+i*(w-52)/(vals.length-1);const y=10+(max-v)/(max-min)*(h-42);i?c.lineTo(x,y):c.moveTo(x,y)});c.stroke();c.fillStyle='#8ea8bf';c.font='11px system-ui';c.fillText(label||key,12,h-9);c.fillText(max.toFixed(max<100?1:0),4,16);c.fillText(min.toFixed(min<100?1:0),4,h-34)}
