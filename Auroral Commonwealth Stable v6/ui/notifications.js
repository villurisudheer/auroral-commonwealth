export function notificationsHTML(s){if(s.settings?.notifications===false)return `<div class="panel"><h3>National Alerts</h3><div class="mini">Notifications are disabled in Settings.</div></div>`;const items=Array.isArray(s.notifications)?s.notifications.slice(0,12):[];return `<div class="panel"><h3>National Alerts</h3>${items.length?items.map(n=>`<div class="notice ${n.type||''}">${escapeHTML(n.text)}</div>`).join(''):'<div class="mini">No active alerts.</div>'}</div>`}
export function pushNotice(s,text,type='information'){if(!Array.isArray(s.notifications))s.notifications=[];s.notifications.unshift({text:String(text??''),type});if(s.notifications.length>40)s.notifications.pop()}
function escapeHTML(x=''){return String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
