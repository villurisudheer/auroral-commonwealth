export function updateCollapse(s){let score=0;if(s.debt/s.gdp>1.7)score++;if(s.inflation>15)score++;if(s.unemployment>18)score++;if(s.approval<25)score++;if(s.stability<30)score++;if(s.energy.production/s.energy.consumption<.75)score++;const prev=s.collapseStage;s.collapseStage=score>=5?'Collapse':score>=4?'Emergency':score>=3?'Crisis':score>=2?'Warning':'Stable';if(prev!==s.collapseStage)s.notifications.unshift({type:s.collapseStage==='Stable'?'information':'crisis',text:`National condition: ${s.collapseStage}`});return s.collapseStage}
